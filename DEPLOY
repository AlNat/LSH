0. [Optional] Delete dubug line in Config.java (Find TODO delete commentary)
1. Run IDEA with Release Configuration [If in war some modules not found -> change the source an its recompiled]
2. In create war need to delete .devmode.js files from war inside folders 
3. Also delete line with devmode in compilations-mapping.txt
4. Update confix.xml file
5. Run this war file with your servlet-server