0. [Optional] Delete dubug line in Config.java (Find TODO delete commentary)
1. Run IDEA with Release Configuration [If in war some modules not found -> change the source and it's recompiled]
2. In create war need to delete *.devmode.js and nocache.js files from war inside folders 
3. Also delete line with Devmode:devmode.js in compilations-mapping.txt in all folders
4. Update confix.xml file
5. Run init.sh script or do it by yourself
6. Run this war file with your servlet-server